If you want to use this Theme put the "Shell" Folder into the Vanilla Boards "theme" subfolder and apply it via the Dashboard.

This Theme is just version 0.1 so it's obviously not quite done. Suggestions welcome.
